<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rtm.com.sysendpoint" >
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/tecnimotor"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Activitys.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name="rtm$com$sysendpoint$Activitys.MenuActivity"  android:label="Menu" android:theme="@style/AppTheme.Dark" />
        <activity android:name="rtm$com$sysendpoint$Activitys.LoginActivity" android:theme="@style/AppTheme.Dark" />
        <activity android:name="rtm$com$sysendpoint$Activitys.SignupActivity" android:theme="@style/AppTheme.Dark" />
        <activity android:name="rtm$com.sysendpoint$Activitys.ClienteActivity" android:theme="@style/Base.AppTheme" >
        <intent-filter>
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
            <intent-filter android:label="@string/filter_title_viewgizmos">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Accepts URIs that begin with "http://www.example.com/gizmos” -->
                <data android:scheme="http"
                    android:host="www.example.com"
                    android:pathPrefix="/gizmos" />
                <!-- note that the leading "/" is required for pathPrefix-->
                <!-- Accepts URIs that begin with "example://gizmos”
                <data android:scheme="example"
                      android:host="gizmos" />
                -->
            </intent-filter>

        </activity>

    </application>

</manifest>
